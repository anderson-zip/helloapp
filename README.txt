Primeiro App
Instalar o Expo-cli:
    npm install -g expo-cli
Instalar o node:
    sudo apt-get install nodejs
Instalar o npm:
    sudo apt-get install npm
Inicializar um aplicação:
    expo init nome_do_projeto 
Instalar dependências:
    npm install 
Start na aplicação
    npm start
